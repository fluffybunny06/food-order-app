# Example environment variables for development
# AWS credentials (you can use the shared AWS credentials file instead)
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key
# AWS region (default used by the app when not set: us-east-1)
AWS_REGION=us-east-1
# S3 bucket used for image uploads (update to your bucket name)
S3_BUCKET=your-s3-bucket-name
